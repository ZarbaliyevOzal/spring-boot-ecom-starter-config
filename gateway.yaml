server:
  port: 8890

spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8891
          predicates:
            - Path=/users/**
          filters:
            - StripPrefix=1